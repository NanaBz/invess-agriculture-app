// This file serves as the entry point for the Tabs component.
// No explicit card or background color logic for roles, but ensure all screens use Colors.background and Colors.card
// All role-based screens reference the same components, so color changes are global.
import Tabs from '../tabs';
export default Tabs;
